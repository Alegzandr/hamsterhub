{% extends 'HamsterHubBundle::base.html.twig' %}

{% block title %}Chaîne de {{ user.username }}{% endblock %}

{% block body %}
    <h3>Vidéos de {{ user.username }}</h3>
    <hr>

    <table>
        {% for video in url %}
            <tr>
                <td colspan="2">
                    <h3>
                        <a href="/watch/{{ video.id }}">{{ video.title }}</a>
                    </h3>
                </td>
            </tr>
            <tr>
                <td>
                    <a href="/watch/{{ video.id }}">
                        <img src="http://img.youtube.com/vi/{{ video.url[32:43] }}/0.jpg" alt="{{ video.title }}"
                             class="thumbnail">
                    </a>
                </td>
                <td>{{ video.description }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}